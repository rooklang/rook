; example rook program
(do
 (set x (env "HELLO"))
 (set n 42)
 (printf x)
 (printf "\n")
 (let (x "test")
      (printf x)
      (printf "\n"))
 (if (eq? x "hi")
     (printf "hello,\tworld\n")
     (printf "goodbye,\tworld\n"))
 ;(print n)
 (print (quote (x y z))))
